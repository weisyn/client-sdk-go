# 集成测试快速开始

---

## 🚀 快速开始（5分钟）

### 1. 启动 WES 节点

打开**终端 1**：

```bash
cd /Users/qinglong/go/src/chaincodes/WES/weisyn.git
bash scripts/testing/common/test_init.sh
```

等待看到：
```
✅ 节点已启动并运行
```

### 2. 运行测试

打开**终端 2**：

```bash
cd /Users/qinglong/go/src/chaincodes/WES/sdk/client-sdk-go.git

# 方式 1：使用测试脚本（推荐）
bash scripts/testing/sdk/token_test.sh

# 方式 2：直接运行 Go 测试
go test ./test/integration/services/token/... -v
```

### 3. 查看测试结果

测试会输出：
- ✅ 测试用例执行状态
- 📊 交易哈希和区块高度
- 💰 账户余额变化
- ❌ 错误信息（如有）

---

## 📋 测试用例说明

### Token 服务测试

- `TestTokenTransfer_Basic` - 基本转账功能测试
- `TestTokenTransfer_InvalidAddress` - 无效地址测试
- `TestTokenTransfer_InsufficientBalance` - 余额不足测试

---

## ⚠️ 常见问题

### 1. 节点未运行

**错误**：
```
节点未运行，请先启动节点
```

**解决**：
```bash
cd /Users/qinglong/go/src/chaincodes/WES/weisyn.git
bash scripts/testing/common/test_init.sh
```

### 2. 测试账户余额不足

**错误**：
```
余额不足
```

**解决**：
- 测试会自动为账户充值（通过挖矿）
- 如果仍然失败，检查节点是否正常出块

### 3. 交易确认超时

**错误**：
```
交易确认超时
```

**解决**：
- 检查节点是否正常运行
- 检查网络连接
- 增加超时时间（修改 `TransactionConfirmTimeout`）

---

## 📚 更多信息

- 测试规划文档：`TESTING_PLAN.md`
- 集成测试说明：`test/integration/README.md`
- 测试脚本说明：`scripts/testing/sdk/README.md`

---

